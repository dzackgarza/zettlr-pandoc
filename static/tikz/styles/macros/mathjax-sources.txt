# Canonical declaration of which macro files are fed into MathJax.
#
# One path per line, relative to this manifest's directory. The MathJax macro
# extractor reads THIS file as its single source of truth for *which* macro
# files to parse — source filenames must NOT be hard-coded in the extractor.
#
# Only MathJax-compatible macro files belong here: tier3 (complex TeX), tier4
# (preamble), environments, and tikz-macros are intentionally excluded because
# MathJax cannot consume them. Blank lines and lines starting with '#' are
# ignored.
tier1-mathjax-simple.tex
tier2-mathjax-args.tex
categories.tex
spectral.tex
